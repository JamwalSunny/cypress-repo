pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'building the app test framework'
            }
        }
            stage('Test') {
            steps {
                echo 'Running testing on app'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy the app'
            }
        }
    }
}
